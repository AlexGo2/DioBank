import { Center } from "@chakra-ui/react";

const Header = () => {
  return (
    <header className="header">
      <Center>
        <h1>Dio Bank</h1>
      </Center>
    </header>
  );
};

export default Header;
